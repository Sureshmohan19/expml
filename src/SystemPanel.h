#ifndef EXPML_SYSTEMPANEL_H
#define EXPML_SYSTEMPANEL_H

#include "Panel.h"

/*
 * Create the System Metrics Panel (Right Column)
 * Populates it with the placeholder data from the screenshot.
 */
Panel* SystemPanel_new(int x, int y, int w, int h);

#endif
